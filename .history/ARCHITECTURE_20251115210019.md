# MessageHub - System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
├─────────────────────────────────────────────────────────────────┤
│  Next.js Frontend (React)                                        │
│  ├─ Pages (19)                                                   │
│  │  ├─ /Login              → Authentication                      │
│  │  ├─ /Dashboard          → Analytics & Stats                  │
│  │  ├─ /Inbox              → Real-time Messages                 │
│  │  ├─ /SendSMS            → Send Messages                      │
│  │  ├─ /Contacts           → Contact Management                 │
│  │  ├─ /Groups             → Group Management                   │
│  │  ├─ /Templates          → Message Templates                  │
│  │  └─ /admin/*            → Admin Tools (6 pages)              │
│  │                                                                │
│  ├─ Components                                                   │
│  │  ├─ AppLayout           → Auth wrapper + nav                 │
│  │  ├─ Admin Components    → Token mgmt, chatroom access        │
│  │  ├─ UI Components       → Shadcn/ui library                  │
│  │  └─ Feature Components  → Forms, tables, cards               │
│  │                                                                │
│  └─ State Management                                             │
│     └─ React Query         → Data fetching & caching            │
└─────────────────────────────────────────────────────────────────┘
                                  ↕
┌─────────────────────────────────────────────────────────────────┐
│                      API LAYER (Next.js)                         │
├─────────────────────────────────────────────────────────────────┤
│  Authentication APIs                                             │
│  ├─ POST /api/auth/login      → Supabase Auth                   │
│  ├─ POST /api/auth/logout     → Clear session                   │
│  └─ GET  /api/auth/me         → Get current user                │
│                                                                   │
│  Token Management APIs (Admin)                                   │
│  ├─ GET    /api/tokens/get    → Get user balance                │
│  ├─ PATCH  /api/tokens/update → Update balance                  │
│  └─ GET    /api/tokens/list   → List all balances               │
│                                                                   │
│  Chatroom Permission APIs (Admin)                                │
│  ├─ GET    /api/user-chatrooms → List assignments               │
│  ├─ POST   /api/user-chatrooms → Assign user                    │
│  └─ DELETE /api/user-chatrooms → Remove assignment              │
│                                                                   │
│  Data APIs (Permission-Filtered)                                 │
│  ├─ GET /api/chatrooms         → List (filtered by role)        │
│  ├─ GET /api/messages          → List (filtered by chatrooms)   │
│  ├─ GET /api/contacts          → List (filtered by chatrooms)   │
│  ├─ GET /api/messages/inbound  → List (filtered by chatrooms)   │
│  └─ POST /api/messages/send    → Send (deducts tokens)          │
│                                                                   │
│  Other APIs                                                      │
│  ├─ /api/users          → User management                       │
│  ├─ /api/groups         → Group management                      │
│  ├─ /api/templates      → Template management                   │
│  ├─ /api/sender-numbers → Twilio number management              │
│  └─ /api/settings       → System settings                       │
└─────────────────────────────────────────────────────────────────┘
                                  ↕
┌─────────────────────────────────────────────────────────────────┐
│                    MIDDLEWARE LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│  Authentication Middleware (lib/authMiddleware.js)               │
│  ├─ getUserFromRequest()     → Extract user from cookie/header  │
│  ├─ requireAuth()            → Protect routes                   │
│  ├─ requireAdmin()           → Admin-only routes                │
│  ├─ checkChatroomAccess()    → Verify chatroom permissions      │
│  └─ getUserChatroomIds()     → Get accessible chatroom IDs      │
└─────────────────────────────────────────────────────────────────┘
                                  ↕
┌─────────────────────────────────────────────────────────────────┐
│                   DATABASE LAYER (Supabase)                      │
├─────────────────────────────────────────────────────────────────┤
│  PostgreSQL Database                                             │
│  ├─ users                    → User profiles (id, email, role)  │
│  ├─ user_tokens              → Token balances (user_id, bal)    │
│  ├─ user_chatrooms           → Permissions (user↔chatroom)      │
│  ├─ chatrooms                → Chat workspaces                  │
│  ├─ contacts                 → Contact directory                │
│  ├─ messages                 → Outbound messages                │
│  ├─ inbound_messages         → Inbound messages                 │
│  ├─ groups                   → Contact groups                   │
│  ├─ group_members            → Group membership                 │
│  ├─ templates                → Message templates                │
│  ├─ sender_numbers           → Twilio numbers                   │
│  └─ settings                 → System settings                  │
│                                                                   │
│  Supabase Auth                                                   │
│  └─ auth.users               → Authentication records           │
│                                                                   │
│  Supabase Realtime                                               │
│  └─ Websocket channels       → Live message updates             │
└─────────────────────────────────────────────────────────────────┘
                                  ↕
┌─────────────────────────────────────────────────────────────────┐
│                   EXTERNAL SERVICES                              │
├─────────────────────────────────────────────────────────────────┤
│  Twilio (SMS Gateway)                                            │
│  ├─ Send SMS                 → Outbound messaging               │
│  └─ Receive SMS (Webhook)    → Inbound messaging               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────┐         ┌──────────────┐         ┌──────────┐
│  Client  │         │   Next.js    │         │ Supabase │
│ (Browser)│         │  API Route   │         │   Auth   │
└────┬─────┘         └──────┬───────┘         └────┬─────┘
     │                      │                      │
     │ 1. POST /api/auth/login                     │
     │    {email, password} │                      │
     ├─────────────────────>│                      │
     │                      │ 2. signInWithPassword│
     │                      ├─────────────────────>│
     │                      │                      │
     │                      │ 3. {session, user}   │
     │                      │<─────────────────────┤
     │                      │                      │
     │                      │ 4. Create user profile
     │                      │    (if not exists)   │
     │                      │                      │
     │ 5. Set cookie +      │                      │
     │    Return user       │                      │
     │<─────────────────────┤                      │
     │                      │                      │
     │ 6. Store token in    │                      │
     │    localStorage      │                      │
     │                      │                      │
     │ 7. Subsequent requests                      │
     │    (with Authorization)                     │
     ├─────────────────────>│                      │
     │                      │ 8. Validate token    │
     │                      ├─────────────────────>│
     │                      │                      │
     │                      │ 9. User data         │
     │                      │<─────────────────────┤
     │                      │                      │
     │ 10. Protected data   │                      │
     │<─────────────────────┤                      │
     │                      │                      │
```

---

## 💰 Token Deduction Flow

```
┌──────────┐         ┌──────────────┐         ┌──────────┐
│  Client  │         │   Messages   │         │ Database │
│          │         │   Send API   │         │          │
└────┬─────┘         └──────┬───────┘         └────┬─────┘
     │                      │                      │
     │ 1. POST /api/messages/send                  │
     │    {to, content}     │                      │
     ├─────────────────────>│                      │
     │                      │ 2. Authenticate user │
     │                      │                      │
     │                      │ 3. Get user role     │
     │                      ├─────────────────────>│
     │                      │<─────────────────────┤
     │                      │                      │
     │                      │ 4. If role='agent':  │
     │                      │    Check balance     │
     │                      ├─────────────────────>│
     │                      │<─────────────────────┤
     │                      │                      │
     │                      │ 5. If balance < 1:   │
     │                      │    Return 402 Error  │
     │                      │    (Insufficient)    │
     │                      │                      │
     │                      │ 6. Send via Twilio   │
     │                      │    (external)        │
     │                      │                      │
     │                      │ 7. Deduct 1 token    │
     │                      ├─────────────────────>│
     │                      │<─────────────────────┤
     │                      │                      │
     │                      │ 8. Store message     │
     │                      ├─────────────────────>│
     │                      │<─────────────────────┤
     │                      │                      │
     │ 9. Success + remaining                      │
     │    tokens            │                      │
     │<─────────────────────┤                      │
     │                      │                      │
```

**Note:** Admins bypass token check (unlimited sending)

---

## 🔒 Permission Filtering Flow

```
┌──────────┐         ┌──────────────┐         ┌──────────┐
│  Client  │         │  Chatrooms   │         │ Database │
│ (Agent)  │         │     API      │         │          │
└────┬─────┘         └──────┬───────┘         └────┬─────┘
     │                      │                      │
     │ 1. GET /api/chatrooms                       │
     ├─────────────────────>│                      │
     │                      │ 2. Authenticate user │
     │                      │                      │
     │                      │ 3. Get user role     │
     │                      ├─────────────────────>│
     │                      │ role='agent'         │
     │                      │<─────────────────────┤
     │                      │                      │
     │                      │ 4. Query user_chatrooms
     │                      │    for user_id       │
     │                      ├─────────────────────>│
     │                      │ [chatroom_id_1,      │
     │                      │  chatroom_id_2]      │
     │                      │<─────────────────────┤
     │                      │                      │
     │                      │ 5. Filter chatrooms  │
     │                      │    WHERE id IN (...)  │
     │                      ├─────────────────────>│
     │                      │<─────────────────────┤
     │                      │                      │
     │ 6. Return only       │                      │
     │    assigned chatrooms│                      │
     │<─────────────────────┤                      │
     │                      │                      │
```

**Applied to:**
- `/api/chatrooms` - List chatrooms
- `/api/messages` - List messages
- `/api/contacts` - List contacts
- `/api/messages/inbound` - List inbound messages

**Admin Bypass:**
If `role='admin'`, skip filtering and return all data.

---

## 📡 Real-Time Messaging Flow

```
┌──────────┐         ┌──────────────┐         ┌──────────┐
│  Inbox   │         │   Supabase   │         │ Database │
│   Page   │         │   Realtime   │         │          │
└────┬─────┘         └──────┬───────┘         └────┬─────┘
     │                      │                      │
     │ 1. Mount component   │                      │
     │                      │                      │
     │ 2. Create channel    │                      │
     │    'inbound-messages'│                      │
     ├─────────────────────>│                      │
     │                      │                      │
     │ 3. Subscribe to      │                      │
     │    postgres_changes  │                      │
     │    (INSERT/UPDATE)   │                      │
     ├─────────────────────>│                      │
     │                      │                      │
     │ 4. SUBSCRIBED ✓      │                      │
     │<─────────────────────┤                      │
     │ (Green indicator)    │                      │
     │                      │                      │
     │                      │ 5. New message       │
     │                      │    inserted          │
     │                      │<─────────────────────┤
     │                      │                      │
     │ 6. Broadcast event   │                      │
     │    {eventType: INSERT}                      │
     │<─────────────────────┤                      │
     │                      │                      │
     │ 7. Invalidate React  │                      │
     │    Query cache       │                      │
     │                      │                      │
     │ 8. Auto-refetch data │                      │
     ├──────────────────────┼─────────────────────>│
     │                      │<─────────────────────┤
     │                      │                      │
     │ 9. Update UI         │                      │
     │    (new message)     │                      │
     │                      │                      │
```

**Features:**
- Live updates without refresh
- Connection status indicator
- Automatic reconnection
- Clean cleanup on unmount

---

## 📊 Data Relationships

```
┌─────────────┐
│    users    │
├─────────────┤
│ id (PK)     │◄────┐
│ email       │     │
│ name        │     │
│ role        │     │
└─────────────┘     │
                    │
      ┌─────────────┴──────────────┐
      │                            │
      │                            │
┌─────▼──────────┐        ┌────────▼─────────┐
│  user_tokens   │        │ user_chatrooms   │
├────────────────┤        ├──────────────────┤
│ user_id (FK)   │        │ user_id (FK)     │
│ balance        │        │ chatroom_id (FK) │─┐
└────────────────┘        └──────────────────┘ │
                                               │
                          ┌────────────────────┘
                          │
                   ┌──────▼────────┐
                   │   chatrooms   │
                   ├───────────────┤
                   │ id (PK)       │◄───┐
                   │ name          │    │
                   │ twilio_number │    │
                   └───────────────┘    │
                                        │
      ┌─────────────┬─────────────┬────┴────────────┐
      │             │             │                  │
┌─────▼──────┐ ┌───▼────────┐ ┌─▼──────────────┐ ┌─▼───────────┐
│  messages  │ │  contacts  │ │ inbound_msgs   │ │   groups    │
├────────────┤ ├────────────┤ ├────────────────┤ ├─────────────┤
│ chatroom_id│ │ chatroom_id│ │ chatroom_id    │ │ chatroom_id │
│ content    │ │ name       │ │ from_number    │ │ name        │
│ from/to    │ │ phone      │ │ content        │ │ contacts[]  │
└────────────┘ └────────────┘ └────────────────┘ └─────────────┘
```

**Key Relationships:**
- Users → user_tokens (1:1)
- Users → user_chatrooms (1:many)
- Chatrooms → user_chatrooms (1:many)
- Chatrooms → messages (1:many)
- Chatrooms → contacts (1:many)
- Chatrooms → inbound_messages (1:many)

---

## 🚀 Deployment Architecture

```
┌──────────────────────────────────────────────────────────┐
│                  Production Environment                   │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │              Vercel / Railway                       │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │         Next.js Application                   │  │  │
│  │  │  - Server-side rendering                      │  │  │
│  │  │  - API routes                                 │  │  │
│  │  │  - Static optimization                        │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────┘  │
│                           ↕                               │
│  ┌────────────────────────────────────────────────────┐  │
│  │              Supabase Cloud                        │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │  PostgreSQL Database                         │  │  │
│  │  │  - Auto-scaling                              │  │  │
│  │  │  - Backup & recovery                         │  │  │
│  │  │  - Row-level security                        │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │  Supabase Auth                               │  │  │
│  │  │  - JWT tokens                                │  │  │
│  │  │  - Email verification                        │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │  Supabase Realtime                           │  │  │
│  │  │  - Websocket connections                     │  │  │
│  │  │  - Postgres CDC                              │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────┘  │
│                           ↕                               │
│  ┌────────────────────────────────────────────────────┐  │
│  │              Twilio Cloud                          │  │
│  │  - SMS sending                                     │  │
│  │  - Inbound webhooks                                │  │
│  │  - Number management                               │  │
│  └────────────────────────────────────────────────────┘  │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

---

## 🔧 Technology Stack

### Frontend
- **Framework:** Next.js 14.2
- **UI Library:** React 18.3
- **Styling:** Tailwind CSS 3.4
- **Components:** Shadcn/ui (Radix UI)
- **Icons:** Lucide React
- **State:** React Query (TanStack)
- **Charts:** Recharts

### Backend
- **Runtime:** Node.js
- **Framework:** Next.js API Routes
- **Auth:** Supabase Auth
- **Database:** PostgreSQL (Supabase)
- **Real-time:** Supabase Realtime
- **SMS:** Twilio

### DevOps
- **Package Manager:** npm
- **Version Control:** Git
- **Deployment:** Vercel/Railway
- **Environment:** .env.local

---

## 📈 Scalability Features

### Horizontal Scaling
- ✅ Stateless API routes
- ✅ Connection pooling (Supabase)
- ✅ CDN for static assets
- ✅ Load balancing (platform-provided)

### Performance Optimizations
- ✅ React Query caching
- ✅ Static page generation
- ✅ Code splitting
- ✅ Image optimization
- ✅ Database indexing

### Multi-Tenancy
- ✅ Row-level security (RLS)
- ✅ User-chatroom isolation
- ✅ Role-based access control
- ✅ Per-user token tracking

---

**Last Updated:** November 15, 2024  
**Version:** 1.0.0
